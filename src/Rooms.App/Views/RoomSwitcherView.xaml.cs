using System.Windows.Controls;

namespace Rooms.App.Views;

/// <summary>Mouse-driven room switcher used inside the tray popup (§12.1).</summary>
public partial class RoomSwitcherView : UserControl
{
    public RoomSwitcherView()
    {
        InitializeComponent();
    }
}
