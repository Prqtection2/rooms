<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Domain layer. Pure C#: models + cross-layer interfaces (ports).
    No package dependencies, no framework coupling. Everything else depends on this.
  -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

</Project>
